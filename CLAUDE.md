# Kimss Java SDK — Claude Code

Read [AI_INTEGRATION.md](AI_INTEGRATION.md). Dual-listener gateway: OpenAI `/v1` and Anthropic `/v1/messages`. Keep native SDKs. Never generate `agents().run` for new integrations.
